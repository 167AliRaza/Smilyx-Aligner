import HomePage from "@/src/components/HomePage";
import { pageMetadata } from "@/src/lib/site";

export const metadata = pageMetadata("Clear Aligners", "Clear aligner treatment with digital planning, clinical supervision, and thoughtful aftercare.");

export default function Page() {
  return <HomePage />;
}
