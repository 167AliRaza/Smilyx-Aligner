import Contact from "@/src/views/Contact";
import { pageMetadata } from "@/src/lib/site";
export const metadata = pageMetadata("Contact", "Contact Smilyx to discuss a clear aligner case, book a meeting, or ask a question.", "/contact");
export default function Page() { return <Contact />; }
